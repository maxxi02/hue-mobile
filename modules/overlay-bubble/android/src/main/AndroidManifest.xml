<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Draw the floating chat-head over other apps. -->
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
  <!-- Keep the overlay alive in the background via a foreground service. -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
  <!-- The ongoing notification the foreground service must post (Android 13+). -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <application>
    <service
      android:name=".BubbleOverlayService"
      android:exported="false"
      android:foregroundServiceType="specialUse">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Floating quick-access bubble for the user's own running session" />
    </service>
  </application>

</manifest>
